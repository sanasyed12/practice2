// CHAPTER - 03 
// Problem - 01
let obj = {
    sana:90,
    ayan:98,
    butool:78,
    ayesha:76
}
for(let i=0;i<Object.keys(obj).length;i++){
    console.log("the marks of " + Object.keys(marks[i]) + " are " + obj[Object.keys(obj)[i]])
}